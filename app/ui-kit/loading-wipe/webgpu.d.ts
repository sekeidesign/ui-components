// WebGPU's globals aren't in TypeScript's DOM lib yet. Referenced here rather
// than through tsconfig's `types`, which would replace the default @types
// discovery instead of adding to it.
/// <reference types="@webgpu/types" />

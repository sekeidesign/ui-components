import { CaseStudyHeader } from "@ui-kit/CaseStudyHeader";
import { Figure } from "@ui-kit/Figure";
import { NextCaseStudy } from "@ui-kit/NextCaseStudy";
import { Problems } from "@ui-kit/Problems";

export const metadata = {
	title: "RAID 2.0 — Tato Case Study | PG Gonni",
	description:
		"A look at RAID 2.0's human-in-the-loop workflow at Tato, and the product foundation that made it possible.",
	openGraph: {
		title: "RAID 2.0 — Tato Case Study | PG Gonni",
		description:
			"A look at RAID 2.0's human-in-the-loop workflow at Tato, and the product foundation that made it possible.",
		images: "/og-image.jpg",
	},
};

<CaseStudyHeader
	eyebrow="Tato · Case study"
	title="RAID 2.0"
	description="Tato pulled six kinds of items out of every meeting, and most of them were noise. RAID 2.0 cut the list to three and put a human in the loop."
/>

_The product and user research behind this project came out of the
[Q2 reset](/case-studies/tato/q2-reset), worth a look for the full context._

Tato extracted six entity types from every meeting: Changes, Risks,
Assumptions, Action Items, Issues, and Decisions. On paper that's thorough.
In practice it produced a wall of overlapping items that most users
scrolled past.

<Problems
	items={[
		"The six types overlapped: a change was usually a decision, an issue was a risk that had already happened.",
		"Extraction produced more items per meeting than anyone could act on.",
		"Items were read-only snippets. If Tato got one slightly wrong, you were stuck with it.",
	]}
/>

## Approach

We replaced the six types with three: Risk, Action Item, Decision. A risk
has an owner and a severity. An action item has an assignee and a due
date. A decision is attributable to a person and tied to the meeting where
it was made. Everything else was cut. The target was blunt: roughly half
the items per meeting, so the list after a meeting is short enough to act
on in a minute.

### A human in the loop

Cleaner extraction only matters if people trust it, so every item became
reviewable. After a meeting, items land in a needs-review list on the
summary. From there the user edits what Tato got slightly wrong, dismisses
false positives, and tracks what matters. Tracked items move to the RAID
page and get monitored across meetings, with badges showing what's new and
what changed since last time. Tato does the reading; the user makes the
calls.

{/* <Figure src="/casestudies/raid-2-0-1.webp" alt="[Alt text]" caption="[Caption]" /> */}

### The rollout

Replacing entity types means touching customers' existing data, so the
rollout became its own project. We ran a month-long transition: a banner
announced the change, the old view stayed the default for two weeks, then
the new view took over with an option to switch back. At the end, tracked
items were migrated with their full history, untracked items were hidden
rather than deleted, and CS got a Control Panel tool to recover old items
on request.

## Outcome

RAID went from a wall of snippets people scrolled past to a short list
they clear after each meeting. Risks persist until someone resolves them,
and decisions carry a paper trail back to the meeting and speaker where
they were made. The project is also the core of Proactive Visibility, an
objective I own: giving customers independent visibility into
implementation risk before sign-off, not after.

<Figure
	src="/casestudies/tato-raid-dashboard.webp"
	alt="Tato RAID dashboard screen"
	caption="RAID 2.0, with a human in the loop"
/>

<NextCaseStudy
	href="/case-studies/tato/meeting-taxonomy"
	title="Meeting taxonomy"
/>
